apiVersion: v1
kind: Service
metadata:
  name: {{ include "sammy-shop.fullname" . }}
  labels:
    {{- include "sammy-shop.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sammy-shop.selectorLabels" . | nindent 4 }}
