apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sammy-shop.fullname" . }}-test-connection"
  labels:
    {{- include "sammy-shop.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sammy-shop.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
